# Copy to .env and fill in (never commit .env).
# Get token: Monday → Profile → Developers → API token

MONDAY_API_TOKEN=
MONDAY_BOARD_ID=18396795757
# Optional hero title (default: Monday board name)
# BOARD_TITLE=eToro Plus — Money Roadmap
# Save API JSON locally after fetch (gitignored): MONDAY_SAVE_SNAPSHOT=1
# Offline regenerate from saved JSON: MONDAY_BOARD_JSON=data/monday-board-snapshot.json
